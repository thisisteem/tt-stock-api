# =============================================================================
# TT Stock API - Environment Configuration
# =============================================================================
# Copy this file to .env and customize the values for your environment
# 
# Quick Start:
# 1. cp .env.example .env
# 2. Generate JWT secret: openssl rand -base64 32
# 3. Set a strong database password
# 4. Start development: make docker-dev

# =============================================================================
# REQUIRED SECURITY VARIABLES (No defaults - must be set)
# =============================================================================

# JWT Secret Key (REQUIRED)
# Must be at least 32 characters long for security
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars

# Database Password (REQUIRED)
# Must be at least 8 characters long
# Use a strong password for production
DB_PASSWORD=your-secure-database-password-change-this

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Database connection settings
# For Docker: Use 'postgres' as host (service name)
# For local: Use 'localhost' as host
DB_HOST=postgres
DB_PORT=5432
DB_NAME=tt_stock_db
DB_USER=tt_stock_user

# Optional: SSL mode for database connection
# Use 'require' for production, 'disable' for development
DB_SSLMODE=disable

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Server port
PORT=8080

# Environment mode (development/production)
ENV=development

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================

# Log level (debug/info/warn/error)
LOG_LEVEL=debug

# Enable hot reload in development (true/false)
HOT_RELOAD=true

# =============================================================================
# DOCKER COMPOSE VARIABLES
# =============================================================================
# These are used by docker-compose.yml and should match the DB_* variables above

POSTGRES_DB=tt_stock_db
POSTGRES_USER=tt_stock_user
POSTGRES_PASSWORD=your-secure-database-password-change-this

# =============================================================================
# PRODUCTION OVERRIDES
# =============================================================================
# For production deployment, override these values:
# ENV=production
# LOG_LEVEL=info
# HOT_RELOAD=false
# DB_SSLMODE=require
# DB_HOST=your-production-db-host

# =============================================================================
# LOCAL DEVELOPMENT OVERRIDES
# =============================================================================
# For local development without Docker, override these values:
# DB_HOST=localhost
# DB_USER=postgres
# DB_PASSWORD=your-local-postgres-password

# =============================================================================
# TESTING CONFIGURATION
# =============================================================================
# For integration tests, you can override these:
# TEST_DB_HOST=localhost
# TEST_DB_PORT=5432
# TEST_DB_NAME=tt_stock_test_db
# TEST_DB_USER=postgres
# TEST_DB_PASSWORD=test_password